# Same physical SQLite file as the main app's DATABASE_URL, in a
# different table ("schedules"). Leave as-is if this process is run
# from the rag_structured/ repo root, same as the main app.
DATABASE_URL=sqlite+aiosqlite:///./data/app.db

HOST=127.0.0.1
PORT=8100
SERVER_NAME=schedule-server
